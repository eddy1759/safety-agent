name: CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    name: 🛠️ Build and Test
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🐳 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🧱 Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          load: true  # Load image to local Docker daemon for use in next steps
          tags: secure-agent:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: 🧪 Run tests inside container
        run: docker run --rm secure-agent:ci pytest

  self-scan:
    name: 🔍 Vulnerability Self-Scan
    runs-on: ubuntu-latest
    needs: build-and-test

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🐳 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🧱 Build Docker image for scanning
        uses: docker/build-push-action@v5
        with:
          context: .
          load: true
          tags: secure-agent:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: 🔍 Scan unsafe dependencies using the agent
        run: |
          docker run --rm \
            -v "${{ github.workspace }}/test_data:/home/agentuser/app/test_data" \
            secure-agent:ci \
            agent scan test_data/unsafe_requirements.txt
